import { HomeContent } from './HomeContent';

export const metadata = {
  title: 'lyve - livestream seller tools',
  description: 'Track inventory, streams, and profitability with real accounting',
};

export default function HomePage() {
  return <HomeContent />;
}
